version: '3'
services:
  api:
    build: .
    image: fastapi
    volumes:
      - "/home/ec2-user/tech-challenge/#01/API:/fastapi"
    ports:
      - "8080:8080"
    command: uvicorn main:app --reload --host 0.0.0.0 --port 8080